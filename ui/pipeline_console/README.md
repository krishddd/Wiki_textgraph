# Pipeline Console

Local web app (served via `textgraph console`) that visualizes an L0->L9 build run as it happens. Built in Phase 6, wired to the same event stream that feeds manifest.json (G7). See Section 6.3 of the build spec.
