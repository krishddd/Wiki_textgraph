"""Evaluation harness (Section 8 / §14).

Real extrinsic benchmarks (LoCoMo, LongMemEval-S, ...) land in Phase 4. Phase 0
ships a runnable stub so the nightly `benchmark.yml` workflow has a valid entry
point and the reporting format is fixed early.
"""
